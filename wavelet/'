% 
A = 0.1:0.1:2;
Ns = length(A)
i=1;
errwi = zeros(1,Ns);
for alpha=A
	subplot(Ns,1,i);
	dw = decwien(testy,K,testnoise,alpha);
	errwi(i) = rele(dw,testyori);
	plot(dw);
	i=i+1;
end
figure
length(errwi)
plot(A, errwi)

